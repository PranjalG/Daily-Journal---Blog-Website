
<%- include("partials/header"); -%>
    <h1>Home</h1>
    <p> <%= startingContent %> </p>
    <% console.log(blog_posts); %>

    <% blog_posts.forEach(function(blog_post) { %>
    <h1> <%= blog_post.title %> </h1>
    <p> <%= blog_post.content.substring(0,150).concat("  ..."); %>
      <a href="/blogPosts/<%= blog_post._id %>">Read More</a>
    </p>
    <% });  %>

    <form action="/compose" method="get">
      <button class = "home-compose-button btn btn-light" type="submit" name="compose" >
        <h5>Compose<i class="fas fa-pen"></i></h5>
      </button>
    </form>



<%- include("partials/footer"); -%>
